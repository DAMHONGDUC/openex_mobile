import 'dart:ui';

class AppColors {
  //dark
  static const Color primary_dark = Color(0xFF080404);
  static const Color text_in_dark = Color(0xFFffffff);
  static const Color text_black = Color(0xFF000000);

  //light
  static const Color primary_light = Color(0xFFffffff);
  static const Color text_in_light = Color(0xFF000000);
}
